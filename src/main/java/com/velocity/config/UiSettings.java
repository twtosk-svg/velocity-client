package com.velocity.config;

/**
 * Menu / overlay UI settings (persisted by ConfigManager).
 */
public final class UiSettings {
    private UiSettings() {}

    /** Menu panel background strength: 0 = fully transparent, 1 = opaque. */
    public static float menuBackgroundOpacity = 0.94f;
}
